<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartautofiller">

        <!-- Splash — LAUNCHER -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Smartautofiller.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- User Info Onboarding -->
        <activity
            android:name=".ui.UserInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartautofiller" />

        <!-- Onboarding — pehli baar -->
        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartautofiller" />

        <!-- Features tour -->
        <activity
            android:name=".ui.FeaturesActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartautofiller" />

        <!-- Privacy Policy -->
        <activity
            android:name=".ui.PrivacyPolicyActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartautofiller" />

        <!-- Main -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Smartautofiller" />

        <activity
            android:name=".ui.CameraActivity"
            android:label="Scan ID Card"
            android:exported="false"
            android:theme="@style/Theme.Smartautofiller" />

        <service
            android:name=".service.SmartAutofillService"
            android:label="Universal AI Autofill"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data android:name="android.autofill" android:resource="@xml/autofill_service_config" />
            <meta-data android:name="android.autofill.settingsActivity" android:value=".MainActivity" />
        </service>

        <service
            android:name=".service.QuickCopyService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <service
            android:name=".service.SmartAccessibilityService"
            android:label="Smart AI Bubble"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice" android:resource="@xml/accessibility_config" />
        </service>

        <receiver android:name=".service.CopyReceiver" android:exported="false" />

        <!-- ✅ Quick Settings Tile -->
        <service
            android:name=".service.AiFillTileService"
            android:label="AI Fill"
            android:icon="@drawable/ic_launcher_foreground"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>

</manifest>